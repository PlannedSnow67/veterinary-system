export interface UserInput {
    name: string;
    email: string;
    password: string;
    phone: string;
    address: string;
    id_role: number;
}

export interface VetInput {
    name: string;
    email: string;
    password: string;
    phone: string;
}